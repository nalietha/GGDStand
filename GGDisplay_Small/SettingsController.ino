/* Will return 1 if the saving was successful. 
// Will return 0 if failed. 
// Serial outout for Debugging
// -- Saved [led color array]
// -- Errored [led color array] \n EEPOM Error Messages?
// -- 
*/ 
// int SaveToEEPOM() {
//   int SavedStatus = false;

//   // Load Current Saved Array: Used to compaire with the current led light array. If they are the same quit out with saved = true (No need to save what is already there.)
//   // 


//   return NotImplemented();
// }


void remoteTurnOff()
{
  // Using the # on the remote toggle the light off/on.
  // 
}

void remoteLightEdit()
{
  // Using the * button start the edit selections.
  // start at index led 0
  // use the <- -> remote arrows to step though.
  // use the ^ and v to large step (up a layer or down a layer)
  // index +- 8 for the large steps
  // walk around when using <- ->
  // OK will start the light edit.
    // 1-9 will be lighting effects
    // up and down on the arrows will change the colors in steps,
    // left right will change the hue
    // How to do that IDK yet
  
}

